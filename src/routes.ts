export const publicRoutes = ["/"]; // Public routes (accessible without login)
export const authRoutes = ["/pages/signin"];
export const apiPrefix = "/api/auth"; // API authentication routes to be skipped
export const DEFAULT_LOGIN_REDIRECT = "/pages/settings";
